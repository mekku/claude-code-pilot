## Kode Brain

This project has a Kode Brain knowledge map at `docs/brain/projects/pilot/`.

Before starting: read `docs/brain/projects/pilot/pilot.md` (project hub) and the
relevant domain pages under `docs/brain/projects/pilot/domains/`.
Check `docs/brain/projects/pilot/reports/reading-packs/` for a pre-built context pack.

KB-first rule: use KB pages as primary source of truth before reading source files.
After editing, note that KB may drift — run `/kodebrain scan` in Claude Code to refresh.
